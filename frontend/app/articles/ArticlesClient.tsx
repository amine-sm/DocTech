"use client";
import { useEffect,useMemo,useState } from "react";
import Link from "next/link";
import Image from "next/image";
import { useSearchParams } from "next/navigation";
import { ChevronRight,Grid2X2,Search,SlidersHorizontal } from "lucide-react";
import Footer from "@/components/Footer";import Header from "@/components/Header";import ProductCard from "@/components/ProductCard";import ShopHero from "@/components/ShopHero";
import { fetchCatalog,fetchCategories,type CatalogCategory,type Product } from "@/lib/catalog";
export default function ArticlesClient(){const sp=useSearchParams();const category=sp.get("categorie");const initialSearch=sp.get("recherche")||"";const [search,setSearch]=useState(initialSearch);const [sort,setSort]=useState("featured");const [products,setProducts]=useState<Product[]>([]);const [categories,setCategories]=useState<CatalogCategory[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");useEffect(()=>{fetchCategories().then(setCategories).catch(()=>{})},[]);useEffect(()=>{const t=setTimeout(()=>{setLoading(true);setError("");fetchCatalog({categorie:category,search,limit:100}).then(r=>setProducts(r.products)).catch(e=>setError(e.message)).finally(()=>setLoading(false))},250);return()=>clearTimeout(t)},[category,search]);const visible=useMemo(()=>{let r=[...products];if(sort==="price-asc")r.sort((a,b)=>a.price-b.price);if(sort==="price-desc")r.sort((a,b)=>b.price-a.price);if(sort==="rating")r.sort((a,b)=>b.rating-a.rating);return r},[products,sort]);const active=categories.find(c=>c.slug===category);const title=category?(active?.label||"Catalogue"):"Tous les articles";return <div className="min-h-screen bg-[#f7f9fd] pb-[76px] text-slate-950 md:pb-0"><Header/><main><ShopHero eyebrow="Catalogue DOCTECH" title={category?title:"Trouvez votre prochain équipement"} description={active?.description||"Découvrez notre catalogue connecté directement à votre base MySQL."} icon={<Grid2X2 size={13}/>}> <div className="mt-7 flex items-center gap-2 text-[11px] font-bold text-slate-500"><Link href="/">Accueil</Link><ChevronRight size={13}/><span className="text-slate-900">{title}</span></div></ShopHero><section className="mx-auto max-w-[1450px] px-4 py-7 sm:px-6 lg:px-8 lg:py-10"><div className="mb-7 overflow-hidden rounded-[26px] border border-slate-200 bg-white p-3 shadow-sm"><div className="flex gap-2 overflow-x-auto pb-1"> <CategoryPill href="/articles" active={!category} label="Tout" image="/images/categories/pc-portable.png"/>{categories.map(c=><CategoryPill key={c.slug} href={`/articles?categorie=${c.slug}`} active={category===c.slug} label={c.label} image={c.image}/>)}</div></div><div className="mb-6 grid gap-3 lg:grid-cols-[1fr_auto]"><div className="relative"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" size={18}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Rechercher un produit, une marque..." className="h-13 w-full rounded-2xl border border-slate-200 bg-white pl-12 pr-4 text-sm font-semibold outline-none focus:border-blue-300"/></div><div className="flex items-center gap-2 rounded-2xl border border-slate-200 bg-white px-3"><SlidersHorizontal size={15} className="text-blue-600"/><select value={sort} onChange={e=>setSort(e.target.value)} className="h-12 bg-transparent text-xs font-black outline-none"><option value="featured">Recommandés</option><option value="price-asc">Prix croissant</option><option value="price-desc">Prix décroissant</option><option value="rating">Mieux notés</option></select></div></div>{error&&<div className="rounded-2xl bg-red-50 p-4 text-sm font-bold text-red-600">{error}</div>}{loading?<div className="grid grid-cols-2 gap-3 md:grid-cols-3 xl:grid-cols-4">{[1,2,3,4,5,6,7,8].map(i=><div key={i} className="h-[390px] animate-pulse rounded-[26px] bg-white"/>)}</div>:visible.length?<><div className="mb-5"><p className="text-[10px] font-black uppercase tracking-[.15em] text-blue-600">Sélection</p><h2 className="mt-1 text-2xl font-black">{visible.length} article{visible.length>1?"s":""}</h2></div><div className="grid grid-cols-2 gap-3 sm:gap-4 md:grid-cols-3 xl:grid-cols-4">{visible.map((p,i)=><ProductCard key={p.id} product={p} index={i}/>)}</div></>:<div className="rounded-[26px] border border-slate-200 bg-white p-12 text-center text-sm text-slate-500">Aucun article trouvé.</div>}</section></main><Footer/></div>}
function CategoryPill({href,active,label,image}:{href:string;active:boolean;label:string;image:string}){return <Link href={href} className={`flex shrink-0 items-center gap-2 rounded-2xl border px-3 py-2 text-[10px] font-black ${active?"border-blue-200 bg-blue-50 text-blue-700":"border-slate-200 bg-white text-slate-600"}`}><span className="relative h-8 w-8 overflow-hidden rounded-lg bg-slate-50"><Image src={image||"/images/categories/pc-portable.png"} alt="" fill sizes="32px" className="object-contain p-1"/></span>{label}</Link>}
