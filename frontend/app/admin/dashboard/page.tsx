"use client";
import { useEffect, useState } from "react";
import { Boxes, CircleDollarSign, PackageSearch, ShoppingCart, Users, Warehouse } from "lucide-react";
import { apiFetch } from "@/lib/api";
import { formatPrice } from "@/lib/catalog";
export default function Dashboard(){const [data,setData]=useState<any>(null);const [error,setError]=useState("");useEffect(()=>{apiFetch<any>("/dashboard/summary").then(r=>setData(r.data)).catch(e=>setError(e.message))},[]);if(error)return <div className="rounded-2xl bg-red-50 p-5 text-sm font-bold text-red-600">{error}</div>;if(!data)return <div className="h-80 animate-pulse rounded-[28px] bg-white"/>;const c=data.cards||{};const cards=[['Utilisateurs',c.users,Users],['Articles',c.articles,Boxes],['Stock faible',c.lowStock,Warehouse],['Commandes',c.orders,ShoppingCart],['Nouvelles',c.newOrders,PackageSearch],['Chiffre d’affaires',formatPrice(Number(c.revenue||0)),CircleDollarSign]] as const;return <div><p className="text-[10px] font-black uppercase tracking-[.16em] text-blue-600">Vue globale</p><h1 className="mt-1 text-3xl font-black tracking-[-.04em]">Tableau de bord</h1><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{cards.map(([label,value,Icon])=><div key={label} className="rounded-[24px] border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.13em] text-slate-400">{label}</p><strong className="mt-2 block text-2xl font-black">{value??0}</strong></div><span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-blue-50 text-blue-600"><Icon size={19}/></span></div></div>)}</div><div className="mt-6 grid gap-5 xl:grid-cols-2"><section className="rounded-[26px] border border-slate-200 bg-white p-5"><h2 className="text-lg font-black">Dernières commandes</h2><div className="mt-4 space-y-2">{(data.recentOrders||[]).map((o:any)=><div key={o.id} className="flex items-center justify-between rounded-xl bg-slate-50 p-3 text-xs"><div><b>{o.tracking_number}</b><p className="mt-1 text-slate-400">{o.customer_name}</p></div><div className="text-right"><b className="text-blue-600">{formatPrice(Number(o.total))}</b><p className="mt-1 text-slate-400">{o.status}</p></div></div>)}</div></section><section className="rounded-[26px] border border-slate-200 bg-white p-5"><h2 className="text-lg font-black">Top articles</h2><div className="mt-4 space-y-2">{(data.topArticles||[]).map((a:any)=><div key={a.article_id} className="flex items-center justify-between rounded-xl bg-slate-50 p-3 text-xs"><b className="max-w-[65%] truncate">{a.product_name}</b><span className="font-black text-blue-600">{a.quantity} vendus</span></div>)}</div></section></div></div>}
