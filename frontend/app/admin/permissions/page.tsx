"use client";import { useEffect,useState } from "react";import { apiFetch } from "@/lib/api";export default function Page(){const [rows,setRows]=useState<any[]>([]);const [error,setError]=useState("");useEffect(()=>{apiFetch<any[]>("/permissions").then(r=>setRows(r.data||[])).catch(e=>setError(e.message))},[]);const groups=rows.reduce((a:any,p:any)=>{(a[p.module]??=[]).push(p);return a},{}) as Record<string,any[]>;return <div><p className="text-[10px] font-black uppercase tracking-[.16em] text-blue-600">Sécurité</p><h1 className="mt-1 text-3xl font-black">Permissions</h1><p className="mt-2 text-sm text-slate-500">Liste des permissions disponibles pour les rôles.</p>{error&&<div className="mt-5 rounded-xl bg-red-50 p-3 text-xs font-bold text-red-600">{error}</div>}<div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{Object.entries(groups).map(([module,items])=><section key={module} className="rounded-[24px] border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-black capitalize">{module}</h2><div className="mt-3 space-y-2">{items.map((p:any)=><div key={p.id} className="rounded-xl bg-slate-50 p-3"><b className="text-xs text-blue-700">{p.code}</b><p className="mt-1 text-[10px] text-slate-500">{p.name}</p></div>)}</div></section>)}</div></div>}
