"use client";import { useEffect,useState } from "react";import { BadgePercent,Sparkles } from "lucide-react";import Footer from "@/components/Footer";import Header from "@/components/Header";import ProductCard from "@/components/ProductCard";import ShopHero from "@/components/ShopHero";import { fetchCatalog,type Product } from "@/lib/catalog";
export default function PromotionsPage(){const [items,setItems]=useState<Product[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{fetchCatalog({promotion:1,limit:100}).then(r=>setItems(r.products)).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <div className="min-h-screen bg-[#f7f9fd] pb-[76px] text-slate-950 md:pb-0"><Header/><main><ShopHero eyebrow="Offres du moment" title="Promotions DOCTECH" description="Promotions actives récupérées directement depuis votre backend MySQL." icon={<BadgePercent size={13}/>}/><section className="mx-auto max-w-[1450px] px-4 py-8 sm:px-6 lg:px-8 lg:py-12"><div className="mb-6 flex items-center justify-between gap-4"><div><p className="text-[10px] font-black uppercase tracking-[.16em] text-red-500">Prix réduits</p><h2 className="mt-1 text-2xl font-black">{items.length} bonnes affaires</h2></div><span className="hidden items-center gap-2 rounded-full bg-red-50 px-3 py-2 text-[10px] font-black text-red-600 sm:flex"><Sparkles size={13}/>Stocks limités</span></div>{error&&<div className="rounded-2xl bg-red-50 p-4 text-sm font-bold text-red-600">{error}</div>}{loading?<div className="grid grid-cols-2 gap-3 md:grid-cols-4">{[1,2,3,4].map(i=><div key={i} className="h-[390px] animate-pulse rounded-[26px] bg-white"/>)}</div>:<div className="grid grid-cols-2 gap-3 sm:gap-4 md:grid-cols-3 xl:grid-cols-4">{items.map((p,i)=><ProductCard key={p.id} product={p} index={i}/>)}</div>}</section></main><Footer/></div>}
