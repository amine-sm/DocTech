"use client";
import { useEffect,useMemo,useState } from "react";
import Image from "next/image";import Link from "next/link";import { useSearchParams,useRouter } from "next/navigation";import { AnimatePresence,motion } from "framer-motion";import { ArrowLeft,Check,CheckCircle2,Heart,Minus,PackageCheck,Plus,ShieldCheck,ShoppingBag,Star,Truck,Zap } from "lucide-react";
import Footer from "@/components/Footer";import Header from "@/components/Header";import ProductCard from "@/components/ProductCard";import { addToCart } from "@/lib/cart";import { fetchCatalog,fetchProductBySlug,formatPrice,type Product } from "@/lib/catalog";
export default function ArticleClient(){const sp=useSearchParams();const router=useRouter();const slug=sp.get("slug")||"";const [product,setProduct]=useState<Product|null>(null);const [related,setRelated]=useState<Product[]>([]);const [selectedImage,setSelectedImage]=useState("");const [qty,setQty]=useState(1);const [favorite,setFavorite]=useState(false);const [added,setAdded]=useState(false);const [loading,setLoading]=useState(true);const [error,setError]=useState("");useEffect(()=>{if(!slug){setError("Article manquant.");setLoading(false);return}setLoading(true);fetchProductBySlug(slug).then(async ({product:p})=>{setProduct(p);setSelectedImage(p.image);const r=await fetchCatalog({categorie:p.category,limit:8});setRelated(r.products.filter(x=>x.id!==p.id).slice(0,4))}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[slug]);const discount=useMemo(()=>product?.oldPrice?Math.round(((product.oldPrice-product.price)/product.oldPrice)*100):0,[product]);function add(){if(!product)return;addToCart(product,qty);setAdded(true);setTimeout(()=>setAdded(false),1200)}function buy(){if(!product)return;addToCart(product,qty);router.push("/panier")}if(loading)return <div className="min-h-screen bg-[#f7f9fd]"><Header/><div className="mx-auto max-w-[1450px] px-4 py-16"><div className="h-[620px] animate-pulse rounded-[30px] bg-white"/></div><Footer/></div>;if(error||!product)return <div className="min-h-screen bg-[#f7f9fd]"><Header/><div className="mx-auto max-w-2xl px-4 py-24 text-center"><h1 className="text-3xl font-black">Article introuvable</h1><p className="mt-3 text-slate-500">{error}</p><Link href="/articles" className="mt-6 inline-flex rounded-2xl bg-blue-600 px-5 py-3 text-xs font-black text-white">Retour au catalogue</Link></div><Footer/></div>;return <div className="min-h-screen bg-[#f7f9fd] pb-[76px] text-slate-950 md:pb-0"><Header/><main><section className="mx-auto max-w-[1450px] px-4 py-6 sm:px-6 lg:px-8 lg:py-10"><Link href="/articles" className="inline-flex items-center gap-2 text-[10px] font-black uppercase tracking-[.14em] text-slate-400 hover:text-blue-600"><ArrowLeft size={14}/>Retour catalogue</Link><div className="mt-6 grid gap-8 lg:grid-cols-2 lg:gap-12"><motion.div initial={{opacity:0,y:18}} animate={{opacity:1,y:0}}><div className="relative overflow-hidden rounded-[30px] border border-slate-200 bg-white shadow-sm">{discount>0&&<span className="absolute left-4 top-4 z-20 rounded-xl bg-red-500 px-3 py-2 text-[11px] font-black text-white">-{discount}%</span>}<button onClick={()=>setFavorite(v=>!v)} className={`absolute right-4 top-4 z-20 flex h-11 w-11 items-center justify-center rounded-full border bg-white/90 ${favorite?"text-rose-500":"text-slate-500"}`}><Heart size={18} className={favorite?"fill-rose-500":""}/></button><div className="relative flex min-h-[390px] items-center justify-center bg-[radial-gradient(circle_at_center,#fff,#eef5ff)] p-7 lg:min-h-[560px]"><AnimatePresence mode="wait"><motion.div key={selectedImage} initial={{opacity:0,scale:.95}} animate={{opacity:1,scale:1}} exit={{opacity:0}} className="relative h-[320px] w-full lg:h-[470px]"><Image src={selectedImage} alt={product.name} fill priority className="object-contain drop-shadow-xl"/></motion.div></AnimatePresence></div></div><div className="mt-3 flex gap-3 overflow-x-auto">{product.gallery.map((img,i)=><button key={i} onClick={()=>setSelectedImage(img)} className={`relative h-20 w-20 shrink-0 overflow-hidden rounded-2xl border bg-white ${selectedImage===img?"border-blue-600 ring-2 ring-blue-100":"border-slate-200"}`}><Image src={img} alt="" fill sizes="80px" className="object-contain p-2"/></button>)}</div></motion.div><motion.div initial={{opacity:0,x:20}} animate={{opacity:1,x:0}} className="lg:pt-2"><div className="flex flex-wrap gap-2"><span className="rounded-full bg-blue-50 px-3 py-1.5 text-[10px] font-black uppercase tracking-[.13em] text-blue-600">{product.brand}</span><span className="rounded-full bg-slate-100 px-3 py-1.5 text-[10px] font-black text-slate-500">{product.categoryLabel}</span></div><h1 className="mt-4 text-3xl font-black leading-[1.12] tracking-[-.045em] sm:text-4xl lg:text-[44px]">{product.name}</h1><div className="mt-5 flex items-center gap-3"><span className="flex items-center gap-1 rounded-xl bg-amber-50 px-3 py-2 text-xs font-black"><Star size={14} className="fill-amber-400 text-amber-400"/>{product.rating}</span><span className="text-xs font-black text-emerald-600">En stock ({product.stock})</span></div><p className="mt-6 text-sm leading-7 text-slate-500">{product.description||"Produit DOCTECH disponible à la commande."}</p><div className="mt-6 rounded-[24px] border border-blue-100 bg-white p-5 shadow-sm"><p className="text-[10px] font-black uppercase tracking-[.16em] text-slate-400">Prix DOCTECH</p><div className="mt-2 flex items-end gap-3"><span className="text-3xl font-black text-blue-600">{formatPrice(product.price)}</span>{product.oldPrice&&<del className="pb-1 text-sm font-bold text-slate-400">{formatPrice(product.oldPrice)}</del>}</div></div><div className="mt-5 grid gap-3 sm:grid-cols-[auto_1fr_1fr]"><div className="flex h-14 items-center rounded-2xl border border-slate-200 bg-white p-1"><button onClick={()=>setQty(v=>Math.max(1,v-1))} className="h-11 w-11 rounded-xl"><Minus size={16} className="mx-auto"/></button><span className="min-w-8 text-center text-sm font-black">{qty}</span><button onClick={()=>setQty(v=>Math.min(Math.max(1,product.stock||99),v+1))} className="h-11 w-11 rounded-xl"><Plus size={16} className="mx-auto"/></button></div><motion.button whileTap={{scale:.98}} onClick={add} className={`flex h-14 items-center justify-center gap-2 rounded-2xl text-xs font-black text-white ${added?"bg-emerald-500":"bg-slate-950"}`}>{added?<Check size={17}/>:<ShoppingBag size={17}/>} {added?"Ajouté":"Ajouter au panier"}</motion.button><motion.button whileTap={{scale:.98}} onClick={buy} className="flex h-14 items-center justify-center gap-2 rounded-2xl bg-blue-600 text-xs font-black text-white"><Zap size={17}/>Acheter maintenant</motion.button></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><Info icon={<Truck size={18}/>} t="Livraison" x="48 wilayas"/><Info icon={<ShieldCheck size={18}/>} t="Garantie" x="Produit garanti"/><Info icon={<PackageCheck size={18}/>} t="Disponible" x="Retrait magasin"/></div></motion.div></div></section><section className="border-y border-slate-200 bg-white"><div className="mx-auto max-w-[1450px] px-4 py-10 sm:px-6 lg:px-8"><h2 className="text-2xl font-black">Caractéristiques</h2><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{(product.features.length?product.features:["Produit contrôlé","Garantie DOCTECH","Assistance disponible"]).map(f=><div key={f} className="flex items-center gap-3 rounded-2xl bg-slate-50 p-4"><CheckCircle2 size={17} className="text-blue-600"/><span className="text-xs font-black text-slate-700">{f}</span></div>)}</div></div></section>{related.length>0&&<section className="mx-auto max-w-[1450px] px-4 py-10 sm:px-6 lg:px-8"><h2 className="text-2xl font-black">Produits similaires</h2><div className="mt-5 grid grid-cols-2 gap-3 md:grid-cols-3 xl:grid-cols-4">{related.map((p,i)=><ProductCard key={p.id} product={p} index={i}/>)}</div></section>}</main><Footer/></div>}
function Info({icon,t,x}:{icon:React.ReactNode;t:string;x:string}){return <div className="flex items-center gap-3 rounded-2xl border border-slate-200 bg-white p-3.5"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-blue-50 text-blue-600">{icon}</span><div><b className="block text-[11px]">{t}</b><span className="text-[10px] text-slate-400">{x}</span></div></div>}
